@model IEnumerable<CentricProject_Team9.Models.EmployeeRecognition>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employee.firstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Recognition.recognitionTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.firstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Recognition.recognitionTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.employeerecognitionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.employeerecognitionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.employeerecognitionID })
        </td>
    </tr>
}

</table>
